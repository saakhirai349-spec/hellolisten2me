index.html
<!DOCTYPE html>
<html>
<head>
    <title>Valentine</title>
    <style>
        body {
            background-color: #f8c8dc;
            text-align: center;
            font-family: Arial, sans-serif;
            margin-top: 100px;
        }

        h1 {
            color: #e91e63;
            font-size: 40px;
        }

        button {
            padding: 15px 30px;
            font-size: 18px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            margin: 10px;
            position: relative;
        }

        #yesBtn {
            background-color: #ff4d6d;
            color: white;
        }

        #noBtn {
            background-color: grey;
            color: white;
            position: absolute;
        }
    </style>
</head>
<body>

    <h1>Zubairu, will u be my valentine? 💖</h1>

    <button id="yesBtn" onclick="yesClicked()">hell yeah 😌</button>
    <button id="noBtn" onmouseover="moveButton()">no 🙄</button>

    <script>
        function moveButton() {
            var button = document.getElementById("noBtn");
            var x = Math.floor(Math.random() * window.innerWidth);
            var y = Math.floor(Math.random() * window.innerHeight);
            button.style.left = x + "px";
            button.style.top = y + "px";
        }

        function yesClicked() {
            alert("ok come over 😏");
        }
    </script>

</body>
</html>
